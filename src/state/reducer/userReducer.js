const userReducer = (state = [], action) => {

    switch(action.type){
        case 'getalluser' : 
        return state;
        default :
        return state;
    }

}

export default userReducer;